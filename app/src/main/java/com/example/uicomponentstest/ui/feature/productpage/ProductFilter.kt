package com.example.uicomponentstest.ui.feature.productpage

import com.example.uicomponentstest.data.model.product.Product

class ProductFilter {
    fun apply(product: Product): Product {
        // TODO
        // get type
        // read filter from matching template json
        // apply filter to product
        // create filtered product
        return product
    }
}